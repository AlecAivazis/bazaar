// external imports
import React from 'react'
// local imports
import { Title } from '../../components'

const Settings = () => (
    <div>
        <Title>Settings</Title>
    </div>
)

export default Settings
