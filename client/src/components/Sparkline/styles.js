// external imports
import { StyleSheet } from 'react-native-web'

export default StyleSheet.create({
    container: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center'
    }
})
